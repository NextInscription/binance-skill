#!/bin/bash
# Binance Ticker 快捷启动脚本

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# 设置代理（可修改）
export HTTPS_PROXY=${HTTPS_PROXY:-http://127.0.0.1:7897}

# 运行
node "$SCRIPT_DIR/binance-ticker.js" "$@"
